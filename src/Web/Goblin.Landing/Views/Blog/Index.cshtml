@{
    ViewData[MetadataKeys.Title] = "Blog";
    ViewData[MetadataKeys.FaviconUrl] = "~/icons/openings.ico";
}

<section class="bg-light py-5">
    <div class="container">
        <div class="row">
            <div class="col">
                @{
                    await Html.RenderPartialAsync("~/Views/Blog/_BigPost.cshtml").ConfigureAwait(true);
                }
            </div>
        </div>
        <div class="row mt-3 mt-lg-5">
            @{
                await Html.RenderPartialAsync("~/Views/Blog/_MiniPost.cshtml").ConfigureAwait(true);
                await Html.RenderPartialAsync("~/Views/Blog/_MiniPost.cshtml").ConfigureAwait(true);
                await Html.RenderPartialAsync("~/Views/Blog/_MiniPost.cshtml").ConfigureAwait(true);
            }
        </div>
    </div>
</section>

<section>
    <div class="container">
        <div class="row">
            @{
                await Html.RenderPartialAsync("~/Views/Blog/_Post.cshtml").ConfigureAwait(true);
                await Html.RenderPartialAsync("~/Views/Blog/_Post.cshtml").ConfigureAwait(true);
                await Html.RenderPartialAsync("~/Views/Blog/_Post.cshtml").ConfigureAwait(true);
                await Html.RenderPartialAsync("~/Views/Blog/_Post.cshtml").ConfigureAwait(true);
                await Html.RenderPartialAsync("~/Views/Blog/_Post.cshtml").ConfigureAwait(true);
                await Html.RenderPartialAsync("~/Views/Blog/_Post.cshtml").ConfigureAwait(true);
            }
        </div>
        <div class="row justify-content-center mt-4">
            <div class="col-auto">
                @{
                    await Html.RenderPartialAsync("~/Views/Blog/_Paging.cshtml").ConfigureAwait(true);
                }
            </div>
        </div>
    </div>
</section>